-- ─────────────────────────────────────────────────────────────────
-- ตำแหน่งย่อยของ Student Assistant (SA) — แต่เดิมตอนเพิ่มสมาชิก SA
-- ไม่ได้ถามตำแหน่งไว้ตั้งแต่แรก จึงเปิดให้แต่ละคนกรอกเองตอนตั้ง PIN
-- ครั้งแรก (ดู app/student/StudentClient.tsx). ฝ่ายอื่น (COPS) มี
-- ตำแหน่งอยู่แล้วผ่านคอลัมน์ department เดิม ไม่ต้องกรอกเพิ่ม.
-- ─────────────────────────────────────────────────────────────────
ALTER TABLE students ADD COLUMN IF NOT EXISTS position TEXT;
